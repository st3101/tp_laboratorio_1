#ifndef BIBLIOTECA_H_INCLUDED
#define BIBLIOTECA_H_INCLUDED


/** \brief Realiza el calculo suma.
 *
 * \param n1 Es el primer numero cargado por el usuario.
 * \param n2 Es el segundo numero cargado por el usuario.
 * \return El resultado de la suma.
 *
 */
float suma(float n1,float n2);

/** \brief Realiza el calcula resta.
 *
 * \param n1 Es el primer numero cargado por el usuario.
 * \param n2 Es el segundo numero cargado por el usuario.
 * \return El resultado de la resta.
 *
 */
float resta(float n1,float n2);

/** \brief Realiza el calculo de la division.
 *
 * \param n1 float Es el primero numero cargado por el usuario.
 * \param n2 Es el segundo numero cargado por el usuario.
 * \return El resultado de la division.
 *
 */
float division(float n1,float n2);

/** \brief reliza el calculo de la multiplicacion.
 *
 * \param n1 float Es el primero numero cargado por el usuario.
 * \param n2 Es el segundo numero cargado por el usuario.
 * \return El resultado de la multiplicacion.
 *
 */
float multiplicacion(float n1,float n2);

/** \brief realiza el calculo del factorial del primer numero.
 *
 * \param n1 float Es el primero numero cargado por el usuario.
 * \return El resultado del primer factorial .
 *
 */
float factorialN1(float n1);

/** \brief realiza el calculo del factorial del segundo numero.
 *
 * \param n2 Es el segundo numero cargado por el usuario.
 * \return El resultado del segundo factorial.
 *
 */
float factorialN2(float n2);


#endif // BIBLIOTECA_H_INCLUDED
